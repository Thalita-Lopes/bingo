


function createSpace(quant){
    if (quant >= 60) {
        for (i = 1; i <= 60; i++){
            let containerBools = document.querySelector('.container-bools')
            let newBool = document.createElement('div')
            newBool.classList.add('bool')
            containerBools.appendChild(newBoll)

            /*criar texto e colocar texto, o texto é o i, pois é o número que corresponde a bola criada*/
        }
    }
}